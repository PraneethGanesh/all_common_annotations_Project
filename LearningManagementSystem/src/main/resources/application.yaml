spring:
  application:
    name: LearningManagementSystem

  datasource:
    url: jdbc:mysql://localhost:3306/lms
    username: ${USER_NAME}
    password: ${PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true